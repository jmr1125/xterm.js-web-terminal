#include "base64.h"
#include "sha1.h"
#include <iostream>
using std::cin;
using std::cout;
int main() {
  string s;
  while (cin >> s) {
    string base64 = encode(to_vector(s));
    cout << "base64: " << base64 << endl;
  }
}
