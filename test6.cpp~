
int main()  {
    vector<bool> result;
    string key = "dGhlIHNhbXBsZSBub25jZQ==";
    key += magickey;
    vector<vector<bool>> Blocks = blocks(to_vector(key));
    vector<DWORD> res = Initabcde;
    for (vector<bool> b : Blocks) {
      res = loop80(blocksGetw(b), res);
    }
    vector<bool> tmp;
    tmp = to_vector(res[0]);
    result.insert(result.end(),tmp.begin(),tmp.end());
  }
