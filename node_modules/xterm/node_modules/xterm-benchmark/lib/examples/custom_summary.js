"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const __1 = require("..");
const helper_1 = require("../helper");
/**
 * Example to show how to add summary stats to baseline and eval
 *   ==>  any key/value pair under .summary is respected if value
 *        contains values and descriptiveStats
 *
 * Example adds:
 *    summary
 *      -- custom*
 *      -- customWithSubs
 *            -- sub1*
 *            -- sub2
 *                  --subsub*
 *
 * (* - stats appearing in final reports)
 */
new __1.RuntimeCase('add summary stats', () => { })
    .postAll((_, perf) => {
    const values = [1, 2, 3, 4, 5];
    perf.summary['custom'] = Object.assign({ values }, helper_1.descriptiveStats(values));
    perf.summary['customWithSubs'] = {
        sub1: Object.assign({ values }, helper_1.descriptiveStats(values)),
        sub2: {
            subsub: Object.assign({ values }, helper_1.descriptiveStats(values))
        }
    };
});
//# sourceMappingURL=custom_summary.js.map