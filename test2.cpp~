#include "socket.h"
#include "uv.h"
#include <iostream>
using namespace std;
int main() {
  Socket sock("127.0.0.1", 9000);
  sock.listen_accept();
  string s;
  for (; !s.size();) {
    string s;
    if(!sock.read(s)){
      cout<<"------"<<endl<<s<<endl;
    }
  }
  return 0;
}
