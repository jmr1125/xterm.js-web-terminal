#include "sha1.h"
#include <iostream>
#include <sys/stat.h>
using namespace std;
ostream &operator<<(ostream &ost, vector<bool>x) {
  ost<<"[";
  for(bool i : x){
    ost<<i<<',';
  }
  return ost;
}
int main() {
  vector<bool> input{
      0, 1, 0, 0, 1, 0, 0, 0, /*H*/
      0, 1, 0, 0, 0, 1, 0, 1, /*E*/
      0, 1, 0, 0, 1, 1, 0, 0, /*L*/
      0, 1, 0, 0, 1, 1, 0, 0, /*L*/
      0, 1, 0, 0, 1, 1, 1, 1  /*O*/
  };
}
